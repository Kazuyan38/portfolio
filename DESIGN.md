# DESIGN.md — デザイン理論と判断の根拠

このサイトのすべてのデザイン判断の「なぜ」を記録する。CLAUDE.md が規則（what）、このファイルが根拠（why）。トークンの値を変更・追加するときは、該当章に根拠を追記してから行う。

## 1. コンセプト — 幽玄と間

**幽玄**: 見せきらないことで生まれる奥行き。このサイトでは「闇の中に月がにじむ」を視覚原理とする。背景アート（`assets/img/bg-yugen.webp`）は主役だが、ベール越しに気配として見せる。全貌を見せた瞬間に幽玄は消える。

**間（ま）**: 余白はコンテンツである。要素を足すか迷ったら引く。余白を詰めるか迷ったら空ける。

この 2 つが上位概念で、以降の章の定量的規則はすべてこれを実現するための下支えである。

## 2. 配色

### アンカーアート法

パレットの出発点は背景アート 1 枚。夜空の濃紺 → ベース、月の白 → テキスト、月光 → アクセント。色に迷ったらアートに戻る。

### 60-30-10 ルール

| 割合 | 役割 | トークン | 伝統色 |
|---|---|---|---|
| 60% | キャンバス（夜の海） | `--c-bg` #041f2f + ベール下のアート | 濃藍（こいあい）近似 |
| 30% | サーフェス（カード・バンド・フッター） | `--c-surface` #0b3348 / `--c-surface-veil` | 藍鉄（あいてつ）近似 |
| 10% | アクセント（リンク・CTA・タブ下線） | `--c-accent` #8fd3e8 | 白群（びゃくぐん）近似 |

テキストは `--c-text` #eaf2f7（月白・げっぱく近似）、控えめテキストは `--c-text-muted` #9db4c2（湊鼠・みなとねず近似）。`--c-accent-2` #93a7ec（紅掛空色近似）は**非テキスト UI 限定**（ホバーボーダー・::selection）。

**アクセントで面を塗らない**: `--c-accent` を使ってよいのはボタン・リンク・タブ下線・フォーカスリング等の小要素のみ。パネルやセクションの背景に塗ると 10% の配分が崩れ、淡色の大面積は夜の海の幽玄を壊す（判断記録: index の CTA パネルをアクセント塗りからサーフェス面 + アクセントボタンに修正）。

### 補助トークンの根拠

- `--c-accent-hover` #b3e2f0 — ホバーは「月光が強まる」方向（明るく）へ寄せる
- `--c-accent-text` #04202f — アクセント面上の文字色。#8fd3e8 上で 10.2:1
- `--c-border` #12405a — 30% サーフェスより一段明るい藍。輪郭は主張させない
- `--c-on-media` / `--c-overlay` — メディア（動画）上の白文字と膜。2 章の実測表が根拠
- `--shadow-soft` / `--shadow-lift` — 夜の闇に浮かぶ影は黒ベース・ぼかし大・オフセット小
- **タグチップは `--c-bg` の「掘り込み」** — 30 のサーフェス面の上では 60 の色で一段沈め、面の前後関係（60 の上に 30、30 の上には 60 の窪み）を保つ

**単一ダークテーマにコミットする**（ユーザー決定）。ライトモードの上書きは置かず、OS のライト設定でも夜の海のまま表示する。幽玄は闇が前提であり、白背景では成立しない — テーマ切替はコンセプトの放棄になるため。

### ベールアーキテクチャ

固定背景アートには左右グラデーションのベールを重ねる。

- `--c-veil-l`（左 = 紙質感の明部側）: **濃く沈める**。テキストが乗る領域のため
- `--c-veil-r`（右 = 夜空と月の側）: **薄く**して月を滲ませる。幽玄の見せ場

**合成計算の規則**: CSS の半透明合成は sRGB ガンマ空間で per-channel（`out = α·veil + (1−α)·img`）。輝度同士の線形ブレンドで見積もると誤る。コントラストは合成後の色を WCAG 相対輝度に変換して算出する。

**コントラスト実測表**（最悪ケース、AA = 4.5:1 以上）:

| 前景 | 背景 | 比 |
|---|---|---|
| text on 素の bg | #041f2f | 14.9:1 |
| muted on veil-l 0.85 ⊗ 最明画素 | ≈rgb(41,64,77) | 5.0:1 ← veil-l を 0.85 未満にしない |
| muted on surface（カード） | #0b3348 | 6.2:1 |
| text on veil-r 0.65 ⊗ 月 | ≈rgb(87,104,115) | 5.1:1 |
| 白 on overlay 0.65 ⊗ 動画白フレーム | ≈rgb(92,109,120) | 5.4:1 ← overlay を 0.65 未満にしない |

**構造的保証**: 月の領域（画像右上）は全ページで不透明ヘッダー + ヒーロー / リード面（surface-veil）に覆われる。muted テキストを素の（透明な）背景に直接置くのは画面の左〜中央のみ。

### 設計判断記録: PHILOSOPHY セクション（`.credo`）

「制作への思い」は philosophy.html を正とし、index の WORKS と CONTACT の間にその抜粋を置く
（WORKS と同じ「index は抜粋 → 専用ページで全文」の型）。判断を記録する。

- **常に 1 カラムで縦に積む**（`.credo-list`、gap は `--sp-5` = グループ境界）。横並びにしない理由は 2 つ。指針は上から順に読ませたいこと、そして 3 カラムだと右カラムの muted テキストが画面右 1/3（月と暗部）に落ち、上の構造的保証が効かないこと。行長は `.measure` で 38 字に抑える
- **ヘアラインは `border-top` ではなく `::before`**。reveal で左から引く（`scaleX(0)→(1)`）ため。`border-width` は transform で伸ばせない。縦積みでは 1 つずつが単独で視界に入るので、線が引かれる動きがそのまま「順に開く」表現になる
- **配置は WORKS と同じサーフェス帯の内側**（波を挟まない）。不透明帯の内側に入れることで、muted テキストのコントラストをベール濃度に依存させない
- **カードにしない**。すぐ上に work-card が 3 枚並ぶため、同じ位置に面をもう 3 つ足すと 30% サーフェスの配分が膨らむ。区切りは `--c-border` の上ヘアライン 1px のみとし、「要素を足すか迷ったら引く」（1 章）に従う
- **番号 01/02/03 は `--c-accent`**。10% は小要素のみという規則の範囲内（3 つの数字は面積として無視できる）で、EN 書体の層（section-title__en / hero__eyebrow）に揃える。意味の順序は持たない装飾なので HTML では `aria-hidden` にし、`<ol>` ではなく `<ul>` を使う
- **追記（モバイル改善）: 番号を丸バッジ化し、縦の接続線でつなぐ**。従来は各指針の上に横 1px ヘアラインを引くだけだったが、モバイルでは文字だけの帯が 3 つ連続して窮屈に見えるとの判断から、`.credo__num` を `--c-accent` ボーダーの円バッジにし、バッジ間を `--c-border` の縦線（`.credo::before`、`scaleY` で reveal）でつなぐ「ステップ図」に変更した。1 カラム縦積み・カード化しない・番号は装飾という既存の判断は維持し、線の向きだけを横→縦（左から右ではなく上から下へ伸びる）に変えている。バッジのボーダー面積は従来の数字と同様に小要素の範囲内

### 設計判断記録: ABOUT の SKILLS を図版グリッドにする

`def-list`（用語1行 + 説明1行を縦積み）は経歴（CAREER、1 行のみ）には合うが、4 項目並ぶ SKILLS では文字だけの帯がモバイルで連続し、窮屈に見えるとの判断から、アイコン付き 2 列カードグリッド（`.skill-grid` / `.skill-card`）に変更した。

- **面（`--c-surface`）を使ってよい根拠**: このセクションは work-card や credo から離れた about.html 内にあり、同一画面に他のサーフェス面が並ばないため 30% 予算を圧迫しない。credo をカード化しなかった判断（work-card と隣接して面が膨らむため）とは前提が異なる
- **アイコンは装飾**。数値化されたスキルレベル（棒グラフ・レーダーチャート等）にしなかったのは、実在しない習熟度を数値で断定すると経歴の誇張になりうるため（CLAUDE.md「仮コンテンツの扱い」）。図形は視覚的な区切りとしてのみ機能させ、情報は従来どおりテキストで持たせる
- **2 列固定**。4 列にすると about.html のサイドバーレイアウト（1024px 以上で 260px アサイド + 本文）と組み合わさったときにカードが狭くなりすぎるため、全ブレークポイントで 2 列のまま gap のみ広げる
- **追記（モバイル改善）: `.skill-card` に `min-height` を固定する**。4 項目の説明文の文字数が揃っていないため、2 列グリッドの行ごとに枠の高さが変わり「青枠が不揃い」に見えていた。CSS Grid は同じ行の中でしか高さを揃えないため（別の行とは揃わない）、最長の説明文（3行相当）が収まる高さを `min-height` で固定し、全カードを同じ高さに揃えた。`.profile__photo` の固定 px と同種の、値を決め打ちする例外

## 3. タイポグラフィ

### モジュラースケール r = 1.25（major third）

基準 16px に比率 1.25 を乗じて導出する。1.333（perfect fourth）は和文の字面の大きさに対して跳びが大きすぎるため 1.25 を採る。

| トークン | 値 | 導出 |
|---|---|---|
| `--fs-xs` | 12px | スケール外。和文可読の下限として意図的に固定 |
| `--fs-sm` | 14px | base ÷ √1.25 のハーフステップ |
| `--fs-base` | 16px | 基準 |
| `--fs-lg` | 20px | r¹ |
| `--fs-xl` | 25px | r² 。**明朝を使ってよい最小サイズ** |
| `--fs-2xl` | 39px | r⁴ |
| `--fs-3xl` | clamp(39px, 7vw, 61px) | r⁴〜r⁶ の流動 |

### 書体の役割分担（対比の原理）

| 層 | 書体 | 適用先 |
|---|---|---|
| アイデンティティ（EN） | `--font-display-en`（Georgia 系セリフ） | section-title__en / hero__eyebrow / site-logo / site-nav__link-en |
| ディスプレイ（和文） | `--font-display-ja`（游明朝系） | hero__title のみ |
| 本文・UI | `--font-base`（ゴシック） | それ以外すべて |

規則:

- **明朝は font-weight 600・`--fs-xl`（25px）以上のみ**。Windows の游明朝は W3 が細く滲むため、600（Demibold に直接マッチ）と大サイズに限定する
- `font-feature-settings: "palt" 1` は display-ja 適用箇所のみ。本文はベタ組み（JIS 組版慣行）
- 本文の行長は `--measure: 38em`（和文 38 字）。行間は本文 1.8、見出し 1.05〜1.4

## 4. 空間

### 8pt グリッド（4px 基準スケール）

`--sp-1`〜`--sp-7`（4/8/16/24/32/48/64px）。中間値を使いたくなったらレイアウトを疑う。

### 近接バンド規則（ゲシュタルトの近接）

| 距離 | 意味 | 例 |
|---|---|---|
| sp-1〜2 | 密結合（同一要素の内部） | 見出し EN と JA サブ |
| sp-3〜4 | 関連（同一グループ内） | 本文段落間、カード内要素 |
| sp-5 | グループ境界 | section__head と本体 |
| sp-6〜7 | セクション間 | セクションの padding |

「関連要素は sp-4 以内、セクション間は sp-6 以上」。この帯を跨ぐ値を使うときは意味の距離を再考する。

### 設計判断記録: `.wrap` のモバイル余白を sp-3 → sp-4 に上げる

初期実装は `.wrap { padding-inline: var(--sp-3) }`（16px）→ 600px 以上で `--sp-4`（24px）だったが、スマホ実機でコンテンツが画面端に迫り窮屈に見えるとの指摘を受け、モバイル基準を `--sp-4`（24px）→ 600px 以上を `--sp-5`（32px）に一段ずつ引き上げた。8pt グリッドの既存トークンの範囲内での変更であり、新しいトークンは追加していない。

### ヒーローの高さ：100vh

初期実装は白銀比 1:√2（`100vh ÷ 1.414` ≈ 71vh、和紙・判型の伝統比）を採っていたが、
ファーストビューが画面に対して短く感じるとの判断により `100vh` に変更した。
`.hero { padding-block: var(--sp-7) }` は border-box のため min-height には計上済みで、
100vh を超えて溢れることはない。

## 5. インタラクション

- **Fitts の法則**: タップターゲットは `--tap-min: 44px` 以上。適用先 = `.btn`、`.site-nav__link`、`.filter__btn`、フッターのリンク
- **Hick の法則**: ナビは 4 項目（上限 5）、絞り込みタブは最大 7。超えるならタグを統廃合する（main.js が超過時に console.warn で警告する）。**PHILOSOPHY の追加でナビは上限の 5 項目に達した**。6 つ目を足すときは、まず既存項目の統合（例: PHILOSOPHY を ABOUT に畳む）を検討する
- **Jakob の法則**: ロゴ左上 = ホームリンク、ナビ右上、下線 = 選択状態（aria-current とタブの下線を類同で揃える）。慣習を破るのはコンセプト上の必然があるときのみ

### 設計判断記録: モバイルナビは EN 表記のみにする

`.site-nav__link` は EN（HOME 等）+ JA（ホーム 等）の 2 行構成だが、5 項目 × 2 行がモバイル幅で折り返し、ナビが縦に何段も伸びて画面を圧迫するとの指摘を受けた。ハンバーガーメニュー化（開閉 JS・Esc・aria-expanded の実装が必要）ではなく、まず表記を 1 行に減らす軽い対応を採った。

- 600px 未満は `.site-nav__link-ja` を `display: none` にし、EN 表記のみで 1 行にする。600px 以上は画面に余裕があるため両方表示する従来どおりの見た目に戻す
- JA を消す分、Fitts の 44px を保つため `.site-nav__link` の `padding-block` をモバイルのみ `--sp-2`→`--sp-3` に上げた（EN 1行 line-height 1.2×14px + padding 16px×2 ≈ 49px）
- ナビ項目数（5）やリンク構造は変えていないため、Hick の法則の上限判断（5章冒頭）はそのまま有効。ハンバーガー化は今回見送ったが、5 項目のままさらに窮屈になる場合の次の選択肢として残す

### 設計判断記録: works の絞り込みは tablist にしない

見た目はタブ（下線インジケータ）だが、セマンティクスは `role="group"` + `aria-pressed` のトグル群を維持する。

- 根拠: WAI-ARIA APG の Tabs パターンは「複数パネルの切替」が前提で、`role="tab"` には `tabpanel` と roving tabindex が必須。この UI は単一リストの絞り込みであり、tab と名乗るとスクリーンリーダーにパネル切替を誤示唆する。支援技術には嘘をつかない
- 矢印キー移動は実装しない。aria-pressed 群の標準操作は Tab / Shift+Tab + Enter/Space。項目 ≤7 なら Tab 移動コストは低く、roving tabindex の実装リスク（フォーカス消失）を避ける

### 設計判断記録: 背景動画に停止ボタンを置かない

WCAG 2.2.2（レベル A）は 5 秒を超える自動動作に停止手段を求めるが、**ヒーローの視覚的な静けさを優先し、停止ボタンは置かない**（ユーザー決定）。ローカル閲覧のみの仮サイトである間の割り切りで、公開時は再検討する（CLAUDE.md の公開チェックリストに項目あり）。

現在も有効な配慮:

- `prefers-reduced-motion: reduce` では CSS が動画を非表示にし、main.js が `autoplay` 属性の除去・`preload="none"`・`pause()` で**ダウンロード自体を止める**（CSS の非表示だけでは 34MB を読み込むため）
- 音声トラックは除去済みで、音が鳴ることはない

### 設計判断記録: 実績カードの動画は contain + 濃藍レターボックス

works の実績動画は縦型 9:16 からシネスコ 2.35:1 まで混在する。カードのメディア枠は 8/5 のまま固定し、`object-fit: contain` で全体を収める（`.work-card__media--video`）。

- cover で切り抜かない: 背景動画と違い**映像の内容が主役**であり、再生中に画面が切れると作品を正しく見せられない。グリッドの秩序（枠 8/5）と作品の完全性は contain で両立する
- レターボックスの余白は黒でなく `--c-bg`（60% のキャンバス色）。「夜の海に映像が浮かぶ」見せ方でコンセプトと整合し、アクセントで面を塗らない規則（2 章）も守る
- **自動再生しない**。`controls` + `preload="metadata"` + `poster` でユーザー操作により再生する。自動再生しないため WCAG 2.2.2（停止手段）の問題はそもそも生じない
- poster は各動画の代表フレームから生成した `.webp`（画像規則に従い 300KB 以下）。トップの実績抜粋カードはこの poster を `<img>` として使い、ヒーロー動画がある index にこれ以上の動画読み込みを積まない
- ホバーズーム（scale 1.03）は動画カードに適用しない。contain で確保した作品の端が `overflow: hidden` で切れ、コントロール操作中に UI が動く「動く標的」にもなるため
- カードの `overflow: hidden` + 角丸はグローバルの外側フォーカスリング（offset 3px）を 3 辺で切り落とす。動画は負の `outline-offset` による内側リングで代替する（フォーカスリングを消さない規則の「見える代替」）

## 6. モーション

幽玄 = 静かに現れ、静かに引く。すべて減速カーブ（`--ease-out`）で「現れる」を表現する。

| トークン | 値 | 用途 |
|---|---|---|
| `--dur-fast` | 0.2s | 色・ボーダーのマイクロ変化 |
| `--dur-base` | 0.3s | カードの持ち上がり等の小移動 |
| `--dur-slow` | 0.45s | メディアのズーム |
| `--dur-enter` | 0.6s | reveal の入場 |
| `--dur-stagger` | 0.25s | 連なって現れるときの遅延。**同時に**現れた中での n 番目 = `--dur-stagger × (n−1)` |
| `--dur-ambient` | 20s | 波の漂い（環境音的モーション） |
| `--ease-out` | cubic-bezier(0.33, 1, 0.68, 1) | 上記すべて（ambient を除く） |
| `--ease-drift` | linear | ambient のみ |

直値の duration / easing を書かない。`prefers-reduced-motion: reduce` では全モーション停止（実装は style.css の Reset 直後のグローバルブロック）。

### 設計判断記録: モバイルはヒーロー動画のパララックス用スケールを最小化する

`.hero__video` の `transform: scale(1.12)` はパララックスの可動域（上下 80px の translate を吸収するための余白）として置いたものだが、`object-fit: cover` と重なることで、スマホの縦長ビューポートでは横方向のクロップが元々大きいところにさらに 12% の拡大が乗り、映像の左右が想定以上に切れて見えるとの指摘を受けた。

- 600px 未満はパララックス自体を止める（`initHeroParallax` が `matchMedia('(min-width: 600px)')` で早期リターン）。可動域が要らなくなるため `.hero__video` の scale もモバイルでは `1.02`（transform の継ぎ目防止に必要な最小限）に下げ、600px 以上でのみ `1.12` に戻す
- `object-fit: cover` 自体によるクロップ（縦長ビューポートに横長素材を敷く以上、原理的に避けられない）はそのまま。今回減らしたのは、そこに上乗せされていたパララックス用の余分な拡大分のみ

### 設計判断記録: style 属性の 2 つの例外

CLAUDE.md の CSS 規則は `style` 属性の直接記述を禁じている。JS が値そのものを決める
必要がある次の 2 箇所のみを例外とし、それ以外は必ずクラスの付け外しで表現する。

1. **`initHeroParallax` の `element.style.translate`** — スクロール量に連動する連続値は
   クラスでは表現できない。`transform` ではなく個別プロパティの `translate` を使うのは、
   CSS 側で確保した `.hero__video { transform: scale(1.12) }`（可動域）と衝突せず
   独立に合成されるため。reduced-motion では JS 側でパララックス自体を起動しない
2. **`initReveal` の `--reveal-i`** — 「同時に現れた中で何番目か」は要素の静的な位置では
   決まらず、IntersectionObserver のコールバック単位でしか分からないため、クラスにできない

### 設計判断記録: reveal の連なりを nth-child で作らない

当初は `[data-reveal-item]:nth-child(n)` に固定の `transition-delay` を置いていたが、これは
**スクロールで 1 つずつ視界に入ったときにも遅延が乗る**。2 番目の要素までスクロールしたのに
`--dur-stagger` 待たされる、という反応の鈍さになる（遅延を大きくするほど悪化するため、
「はっきり順番に見せる」ことと両立しない）。

そこで遅延の根拠を「DOM 上の位置」から「同時に現れたかどうか」に変えた。main.js が
同一コールバック内で交差した要素にだけ 0,1,2… を振り、CSS は
`transition-delay: calc(var(--dur-stagger) * var(--reveal-i, 0))` で受ける。単独で現れた
要素は常に 0 になり遅延ゼロ。複数が同時に入ったときだけ連なる。この設計なら
`--dur-stagger` を大きくしても反応の鈍さが出ないため、0.25s まで上げられる。

### 設計判断記録: index の実績カルーセル

index の WORKS を「3 枚の抜粋グリッド」から「全作品の横スクロール」に変えた。

- **カードは索引に絞る**（ポスター画像 + タイトル + タグ）。works.html と同じ説明文・
  制作年まで載せるとカードが縦に伸びて横送りが重くなるうえ、同じ本文が 2 ファイルに
  増えて差し替え漏れの温床になる。説明文は works.html を正とし、カードは
  `works.html#work-<slug>` へアンカーで送る
- **動画は載せない**。トップにはヒーロー動画があり、ここに 6 本の `<video>` を足すと
  読み込みが重くなる。すべてポスター画像 + `loading="lazy"`
- **カード幅は `clamp(15rem, 72vw, 20rem)`**。固定 px を持たせない規則の範囲で、狭い画面
  では次のカードの端が覗いて「横に続く」と分かり、広い画面でも間延びしない
- **reveal はコンテナに 1 つ**。21 枚に `data-reveal-item` を付けると `--reveal-i` が
  積み上がり、最後のカードが出るまで数秒かかる
- **`overflow-x` は `overflow-y` も `auto` にする**。`.work-card:hover` の
  `translateY(-4px)` と影が切れるため、`.carousel__track` に上下の逃げを取っている
- **キーボード操作**: スクロール領域に `tabindex="0"` と `role="region"` + ラベルを静的に
  置く（Firefox 以外は overflow 要素を自動でフォーカス可能にしないため、これが無いと
  キーボードだけで横に送れない）。JS 無効でも効くので静的に書いてよい。一方で
  「前へ / 次へ」ボタンは操作 UI なので main.js が生成する

### 設計判断記録: ヘッダーの sticky 化とスクロール連動縮小

ヘッダーは常設ナビとして画面上部に留まる方が回遊性が高いと判断し、`position: sticky` にした。
スクロールで背景の影と padding を縮める演出は `--dur-base` / `--sp-2` など既存トークンのみで
構成し、新しいトークンは追加していない。

## 7. 運用

- CLAUDE.md = 規則（what）。DESIGN.md = 根拠（why）
- 新しいスタイルを書く前に該当章の規則を引く。規則に無い判断をしたら、この文書に追記してから実装する
- トークンの値を変える場合、2 章の実測表（コントラスト）と 3 章の導出式（スケール）の整合を保つ
